language: generic

sudo: required
services:
  - docker

before_install:
  - docker build -t neerajbadlani/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true neerajbadlani/docker-react npm run test
